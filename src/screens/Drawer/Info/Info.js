import React from 'react';
import {Text} from 'react-native';
import {SafeAreaView} from 'react-native-safe-area-context';

const Info = () => (
  <SafeAreaView style={{flex: 1}}>
    <Text>DrawerInfo</Text>
  </SafeAreaView>
);

export default Info;
